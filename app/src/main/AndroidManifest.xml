<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="id.itborneo.facecare">

    <uses-feature android:name="android.hardware.camera.any" />

    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FaceCare">
        <activity android:name=".daytoday.DayToDayActivity"></activity>
        <activity android:name=".community.CommunityActivity" />
        <activity android:name=".analyzing.AnalyzingActivity" />
        <activity android:name=".article.views.DetailArticleActivity" />
        <activity android:name=".faceproblem.FaceProblemActivity" />
        <activity android:name=".herbal.HerbalActivity" />
        <activity android:name=".product.ProductActivity" />
        <activity android:name=".identify.IdentifyActivity" />
        <activity android:name=".auth.register.RegisterActivity" />
        <activity android:name=".auth.login.LoginActivity" />
        <activity android:name=".result.ResultActivity" />
        <activity android:name=".main.MainActivity" />
        <activity android:name=".article.views.ArticleActivity" />
        <activity android:name=".onboarding.AppIntroActivity" />
        <activity android:name=".splash.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>